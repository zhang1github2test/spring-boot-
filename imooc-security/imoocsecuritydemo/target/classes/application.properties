## 数据库连接配置
spring.datasource.driver-class-name: com.mysql.jdbc.Driver
spring.datasource.url: jdbc:mysql://127.0.0.1:3306/zhangshenglu?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
spring.datasource.username: root
spring.datasource.password: 123

spring.session.store-type=none

security.basic.enabled=false
## 该配置节点为独立的节点，有很多同学容易将这个配置放在spring的节点下，导致配置无法被识别
mybatis:
  mapper-locations: classpath:com/itmuch/cloud/mapping/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
  type-aliases-package: com.winterchen.model  # 注意：对应实体类的路径

spring.thymeleaf.cache=false